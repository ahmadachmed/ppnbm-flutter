import 'package:flutter/material.dart';

const primaryColor = Color(0xFFEF6400);
const darkBlackColor = Color(0xFF232F35);
const bgColor = Color(0xFFFEFEFE);
const bodyTextColor = Color(0xFF232F35);

const defaultPadding = 15.0;
const maxWidth = 1080.0;
const animationDuration = Duration(milliseconds: 200);
